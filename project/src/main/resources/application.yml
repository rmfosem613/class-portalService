spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/potal_project
  thymeleaf:
    prefix: classpath:/templates/  # 템플릿 파일의 경로 설정
    suffix: .html  # 템플릿 파일의 확장자 설정
  web:
    resources:
      static-locations: classpath:/static/
